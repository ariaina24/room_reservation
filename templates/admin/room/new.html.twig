{% extends 'admin/layout.html.twig' %}

{% block admin_content %}
<h1 class="text-3xl font-bold text-gray-800 mb-8">
    ➕ Ajouter une nouvelle salle
</h1>

<div class="bg-white p-8 rounded-xl shadow-lg border border-gray-200">
    {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

            <div>
                <label class="block text-sm font-semibold text-gray-700 mb-1">Nom de la salle</label>
                {{ form_widget(form.name, {'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none'}}) }}
                {{ form_errors(form.name) }}
            </div>

            <div>
                <label class="block text-sm font-semibold text-gray-700 mb-1">Capacité</label>
                {{ form_widget(form.capacity, {'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none'}}) }}
                {{ form_errors(form.capacity) }}
            </div>

            <div class="md:col-span-2">
                <label class="block text-sm font-semibold text-gray-700 mb-1">Localisation</label>
                {{ form_widget(form.localisation, {'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none'}}) }}
                {{ form_errors(form.localisation) }}
            </div>

            <div class="md:col-span-2">
                <label class="block text-sm font-semibold text-gray-700 mb-1">Description</label>
                {{ form_widget(form.description, {'attr': {'class': 'w-full h-32 px-4 py-2 border border-gray-300 rounded-lg resize-none focus:ring-2 focus:ring-blue-500 focus:outline-none'}}) }}
                {{ form_errors(form.description) }}
            </div>

            <div class="md:col-span-2">
                <label class="block text-sm font-semibold text-gray-700 mb-1">Image de la salle</label>
                {{ form_widget(form.image, {'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-lg bg-white file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100'}}) }}
                {{ form_errors(form.image) }}
            </div>

        </div>

        <div class="mt-8">
            <button type="submit" class="px-6 py-3 bg-blue-600 text-white font-medium rounded-lg shadow hover:bg-blue-700 transition duration-200">
                💾 Enregistrer la salle
            </button>
            <a href="{{ path('app_room_index') }}" class="ml-4 inline-block px-5 py-2 bg-gray-200 text-gray-700 font-medium rounded hover:bg-gray-300 transition">
                🔙 Retour
            </a>
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}
